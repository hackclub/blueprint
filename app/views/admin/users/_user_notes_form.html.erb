<%= turbo_frame_tag "user_notes_#{user.id}" do %>
  <div id="user_notes_flash_#{user.id}" class="mb-4">
    <% if flash[:notice] %>
      <div class="p-3 bg-bp-success/20 border-2 border-bp-success rounded text-bp-success">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <% if flash[:alert] %>
      <div class="p-3 bg-bp-danger/20 border-2 border-bp-danger rounded text-bp-danger">
        <%= flash[:alert] %>
      </div>
    <% end %>
  </div>
  
  <%= form_with model: user, url: update_internal_notes_admin_user_path(user), method: :patch, html: { class: "space-y-4" } do |f| %>
    <%= f.hidden_field :frozen_internal_notes, value: user.internal_notes %>
    <%= f.text_area :internal_notes, rows: 3, placeholder: "Add internal notes about this user...", class: "input px-3 block w-full", value: user.internal_notes %>
    <%= f.submit "Save Notes", class: "btn btn-primary" %>
  <% end %>
<% end %>
